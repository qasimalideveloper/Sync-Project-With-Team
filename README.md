# Sync-Project-With-Team
A small software that can be used to sync the project files with your project team with just one click
# How to use it
1. Go inside sync folder and install requirements by using this command:
```
pip install -r requirements.txt
```
2. Run the python file "sync.py" using this command
```
python sync.py
```
